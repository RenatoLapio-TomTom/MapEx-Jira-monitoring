name: Jira Weekly Monitoring

on:
  schedule:
    - cron: "0 18 * * 6"
  workflow_dispatch:
    inputs:
      backfill_weeks:
        description: "Weeks to backfill (e.g. W28,W29,W30) — leave empty for current week"
        required: false
        default: ""

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Collect Jira data
        env:
          JIRA_BASE_URL: ${{ secrets.ATLASSIAN_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.ATLASSIAN_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.ATLASSIAN_API_TOKEN }}
        run: |
          if [ -n "${{ github.event.inputs.backfill_weeks }}" ]; then
            python scripts/collect_jira_data.py --backfill "${{ github.event.inputs.backfill_weeks }}"
          else
            python scripts/collect_jira_data.py
          fi

      - name: Update Confluence pages
        env:
          CONFLUENCE_BASE_URL: ${{ secrets.ATLASSIAN_BASE_URL }}/wiki
          CONFLUENCE_EMAIL: ${{ secrets.ATLASSIAN_EMAIL }}
          CONFLUENCE_API_TOKEN: ${{ secrets.ATLASSIAN_API_TOKEN }}
        run: python scripts/update_confluence.py

      - name: Commit updated CSV
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/weekly_snapshots.csv
          git diff --staged --quiet || git commit -m "Weekly Jira snapshot - $(date +'%Y-W%V')"
          git push
